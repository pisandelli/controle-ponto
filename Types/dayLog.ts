export type DayLog = {
    key: string
    entrada: string
    pausaInicio: string
    pausaFim: string
    saida: string
    obs: {
      entrada: string
      pausaInicio: string
      pausaFim: string
      saida: string
    }
}